Static Assets Folder
====================

Any assets placed in this folder (e.g., logos, images, icons, or audio) will be served directly at the root URL path.

For example:
- A file named `public/logo.png` is referenceable in your code as `/logo.png` or `src="/logo.png"`.
- This works exactly like the `public` folder in Next.js.
